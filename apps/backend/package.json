{
  "name": "@claimsahayak/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Content & event backend scaffold (V3 §1.1 unit 2). Deliberately dependency-free in M1: env parsing, health endpoint, graceful shutdown. Rule-pack storage, publish pipeline, and event ingestion land in Milestones 10–11.",
  "type": "module",
  "main": "./dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "20.17.10",
    "tsx": "4.19.2",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
